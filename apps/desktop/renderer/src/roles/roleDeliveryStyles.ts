import { roleTileFieldClass } from "./roleEditorStyles";

/** Shared field style for role delivery settings rendered on tinted tiles. */
export const roleDeliveryFieldClass = roleTileFieldClass;

/** Shared compact icon action style for ordered delivery rows. */
export const roleDeliveryIconButtonClass = "grid h-8 w-8 place-items-center rounded-md text-ink-muted transition hover:bg-white/70 hover:text-ink focus:outline-none disabled:cursor-not-allowed disabled:text-ink-faint";
